---
# Ansible Inventory — Forensic Analysis System
# Installiert alle Abhängigkeiten auf dem lokalen Rechner (localhost)

all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
